<div class="ms-Grid">
  <div class="ms-Grid-row">
    <!-- App navigation bar markup. -->
    <div class="ms-NavBar">
      <ul class="ms-NavBar-items">
        <li class="navbar-header">Microsoft Graph Connect sample</li>
      </ul>
    </div>

    <!-- App main content markup. -->
    <div class="ms-Grid-col ms-u-mdPush1 ms-u-md9 ms-u-lgPush1 ms-u-lg6">
      <div>
        <h1 class="ms-font-xl">{{name}}'s File</h1>
         <h1 class="ms-font-xl">All file from One Drive</h1>
            {{#each file}}
            <li class="ms-font-m">{{id}}</li>
            <p class="ms-font-m">{{name}}</p>
            <hr/>
            {{/each}}
        </ul>
      </div>
    </div>
  </div>
</div>